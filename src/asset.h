#pragma once
#include <glm/glm.hpp>
#include <vector>

std::vector<float> getVertices(const glm::vec3& positions,
                               const glm::vec3& color);
                               